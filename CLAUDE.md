# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

本檔案提供給 Claude Code（或其他 AI 工具）在此 repo 中工作時的上下文說明。

## 專案簡介

一個以 Konva.js 為核心的線上白板/繪圖工具，用 Next.js (App Router) + React 建置。
核心功能（規劃中）：畫筆、橡皮擦、上傳圖片、加文字，圖片與文字皆可拖曳、縮放、旋轉。
目前已實作的部分：畫布可自動縮放置中（依容器大小 fit，滑鼠滾輪縮放已停用，待另外設計「調整畫布大小」功能）、從左側 Template 面板新增矩形。

## 技術棧

- Next.js 16 (App Router, TypeScript)、React 19
- konva / react-konva（畫布渲染，僅能在 Client Component 使用）
- @dnd-kit/core / @dnd-kit/sortable / @dnd-kit/utilities（`LayersPanel` 圖層拖曳排序用，見下方架構重點）
- 樣式分兩層：
  - `src/app/globals.css` 用 Tailwind v4（`@import "tailwindcss"`）設定全站 base theme（字型、明暗色變數）。
  - 各元件實際樣式用 **SCSS Modules**（`*.module.scss`），共用變數（顏色、尺寸）集中在 `src/styles/global.scss`，元件內用 `@use "../../styles/global" as *` 引入 —— 新增元件樣式請沿用這個模式，不要在元件內直接寫 Tailwind class。
- Path alias：`@/*` → `src/*`（見 `tsconfig.json`）。

## 開發指令

```bash
npm run dev       # 啟動開發伺服器 (localhost:3000)
npm run build     # 打包正式版
npm run start     # 啟動正式版 server
npm run lint      # 執行 ESLint（eslint-config-next，flat config）
```

目前專案沒有設定測試框架（無 test script、無 jest/vitest/playwright），若要新增測試需求請先跟使用者確認要用哪一套。

## 架構重點

- **狀態集中在 Context**：`src/context/CanvasContext.tsx` 的 `CanvasProvider`/`useCanvas()` 是畫布的單一狀態來源，管理 `shapes: CanvasShape[]`（`RectShape | ImageShape | TextShape` union，靠 `type` 判別欄位區分，單一扁平陣列，不拆成 `shapes`/`texts`/`images` 三個陣列——`useShapeSelection`/`KonvaBoard` 的渲染迴圈、Transformer 共用邏輯都靠「遍歷同一個陣列＋用 id 查」運作，拆開只會讓這些地方多維護好幾份查找邏輯）、`canvasWidth`/`canvasHeight`（+`setCanvasSize`，畫布尺寸也是 state 而非寫死常數，為之後「修改畫布大小」功能預留）、`selectedIds: string[]`（目前選中的物件，支援多選，設計成跨元件共享；`selectedId` 是 `selectedIds.length === 1` 時的衍生值，只讀不能直接 set，給只在乎單一選取物件的既有元件用）、`activeId: string | null`（使用者點擊「真的展開了鎖定分組」時、被點的那一個 shape id，跟 `selectedIds` 是刻意分開的兩個狀態——`panelRight` 的 `SelectedShapePanel` 只要 `activeId` 還在 `selectedIds` 裡就顯示它的可編輯 `ShapePropertiesForm`，否則（`activeId` 為 `null`）`selectedIds.length >= 2` 時顯示「N shapes selected」批次刪除。**`activeId` 只在分組展開時才設成非 null**，一般多選（shift/ctrl+click 點到沒分組的 shape，`selectedIds` 單純疊加）刻意維持 `null`——這是修正過的行為：一開始曾經寫成「只要點的 id 還留在最終選取裡就設 activeId」，結果連一般 shift+click 累加出的多選都被誤判成「有明確目標」，讓「N shapes selected + 批次刪除」這個既有介面在最常見的多選流程下完全顯示不出來，之後改成只看「這次點擊的選取單位是不是真的因為分組而展開（`idsToToggle.length > 1`）」才會設，見下方獨立條目）、`selectShape(id, additive)`（畫布點擊用的選取切換：`additive` 為 false 直接取代選取，true 時把選取目標加入/移出目前選取；點到鎖定分組的成員時選取單位會展開成整個分組，這時才把 `activeId` 設成被點的那一個，見下方獨立條目）、`selectShapeExact(id, additive)`（圖層清單列點擊專用，語意跟 `selectShape` 一樣但永遠只針對這個 id 本身、不展開成整組，`activeId` 因此永遠設 `null`——`LayersPanel` 已經是「列出每個獨立 shape」的介面，點特定一列就是想針對那一個 id 操作，即使它屬於鎖定分組也一樣，這樣才能單獨編輯分組裡某個成員的 `x`/`y`/`fill` 等屬性；`selectedIds.length === 1` 時 `SelectedShapePanel` 本來就會 fallback 用 `selectedIds[0]`，不需要 `activeId`）、`lockShapes`/`unlockShapes`（圖層鎖定分組，見下方獨立條目）、`updateShapes(patches)`（`updateShape` 的批次版本，一次操作改多個 shape 只推一筆 history entry，鎖定分組的畫布拖曳連動、對齊功能都靠這個，見下方獨立條目）、`alignShapes(ids, mode)`（對齊選取物件到畫布邊界/中心，見下方獨立條目）、`updateShape(id, patch: ShapePatch)`（依 id 淺層合併寫回 shape；`ShapePatch` 是手寫的攤平型別，把三種 shape 可能用到的欄位都列成 optional，不是用 `Omit<...> & Omit<...>` 交集推導——物件型別交集算出來的是欄位聯集不是共同欄位，容易誤導）、`deleteShape(id)`（單刪，內部是 `deleteShapes([id])` 的薄包裝）、`deleteShapes(ids)`（批次刪除，一次使用者操作只推一筆 history entry，不逐個 id 各推一筆）、`resetCanvas()`（清空 `shapes`，`shapes.length === 0` 時內部直接 no-op，不推無意義的 history）、`containerRef`（`KonvaBoard` 量測容器尺寸用的 DOM ref，放在 context 是為了之後 Toolbar 做「匯出畫布」時能拿到同一個節點，`Toolbar` 跟 `KonvaBoard` 是平行元件不能直接互傳 ref）。新增物件（`addSquare`/`addText`/`addImage`）都會呼叫 `nextId(prefix)` 產生 `shape-1`/`text-1`/`image-1`/`group-1` 這種每種類型各自遞增、不會歸零的 id（不受 undo/redo/reset 影響，永遠只增不減，避免復原舊 shape 後跟新 id 撞號），並且新增後自動 `setSelectedIds([id])`，讓使用者能立刻拖曳/調整剛新增的物件。
- **Undo/Redo 已實作，邏輯直接寫在 `CanvasProvider` 內，沒有另外開 `useCanvasHistory.ts`**：`past`/`future` 存 `CanvasSnapshot[]`**物件陣列**（不是 JSON 字串）——`ImageShape.src` 可能是幾百 KB～幾 MB 的 base64，存字串的話每次 push 都要整包重新 `JSON.stringify`、且字串間不共享記憶體；存物件則沿用現有的 immutable 更新寫法（`[...prev, x]`/`.map` 只換命中的元素），未變動的 shape（含大 base64）在多個 history entry 之間是同一個物件參考，不會被複製。**這個設計的前提是：往後修改 `shapes` 的程式碼都必須「不原地 mutate、只能整個換新物件/陣列」**，否則舊的 history entry 會被意外改到。每個會修改狀態的 action（`addSquare`/`addText`/`addImage`/`updateShape`/`deleteShape`/`deleteShapes`/`lockShapes`/`unlockShapes`/`resetCanvas`/`setCanvasSize`）都會在真正 `setShapes`/`setCanvasWidth`/`setCanvasHeight` 之前呼叫 `pushHistoryEntry()`（push 進 `past`、清空 `future`）。`undo`/`redo` 直接讀 `past`/`future` state（不是在 `setPast`/`setFuture` 的 updater 裡塞副作用），呼叫 `applySnapshot` 整包還原並用 `setSelectedIds` 的 functional updater 把「還原後已經不存在的選取 id」濾掉（多選友善版本，不是單純判斷單一 id 還在不在）。**`selectedIds` 不算進 history**（切換選取是暫態 UI 狀態）——所以 undo 一個刪除動作後 shape 會復原，但 `SelectedShapePanel` 的 pill 不會自動重新出現，這是設計上的預期行為。跟這套 undo 機制完全獨立的是 `snapshotRef`（`useRef<string>`）+ `useEffect` 序列化出的 `getSnapshot(): string`，純粹留給未來 Export 功能讀取目前狀態，兩套機制不共用同一份資料。
- **選取/拖曳/縮放/框選邏輯在 `src/hooks/useShapeSelection.ts`**：用 `id: string` 操作、不寫死 shape 類型，矩形/文字/圖片共用同一支 hook。`selectedIds` 本身不是這支 hook 的 local state，是讀寫 `CanvasContext` 的 `selectedIds`（多選）——hook 只負責「選中後 Konva 層怎麼把共用 `Transformer` 掛上去、怎麼處理拖曳/縮放/框選事件」。`handleTransformEnd` 依 `node.getClassName()` 分流成四種寫回方式：`Text` 只改 `fontSize`（`TextShape` 沒有 `width`/`height`，靠 Konva.Text 依內容自動算）；`Star` 只改 `size`（`node.width()` 讀到的是 Konva 內建換算出的「半徑 × 2」，`Star` 強制鎖了 `keepRatio` 所以 `scaleX === scaleY`，這次改動刻意不動它，見下方新形狀條目）；`Line` 改 `points`（陣列每個座標依奇偶索引分別乘 `scaleX`/`scaleY`，不是改 `width`/`height`，`Line` 根本沒有實質的這兩個欄位）；其餘（`rect`/`image`/`circle`/`triangle`）改 `width`/`height`——**`Circle`/`RegularPolygon`（三角形）這次併入這個分支**，靠 `KonvaBoard.tsx` 把它們的 `radius` 固定成常數 `SHAPE_BASE_RADIUS`、實際 `width`/`height` 全部改由 `scaleX`/`scaleY` 表現（見下方新形狀條目的技術細節），`node.width()`/`node.height()` 因此永遠讀到同一個常數直徑，乘上 `scaleX`/`scaleY` 就是新的實際尺寸，跟 rect/image 完全共用同一段公式、不用另外分支。四個分支都有下限 clamp（避免縮到 0 或消失），也都會讀 `node.rotation()`（`Math.round` 成整數）一起 `updateShape`——所有 `CanvasShape` 型別跟 `ShapePatch` 都有 `rotation: number`，`Transformer` 的 `rotateEnabled` 已開啟，不做 0–360 正規化。**`handleTransformEnd` 是掛在每個 shape 自己的 `onTransformEnd`（`KonvaBoard.tsx` 裡 `commonHandlers` 的一員），不是掛在 `<Transformer>` 上**——這個設計選擇是多選能「零成本」支援縮放/旋轉的關鍵：Konva 的 `Transformer.nodes([...])` attach 多個節點時，會對每個節點各自套用群組變換並各自觸發原生 `transformend`，所以就算選取變成陣列，每個 shape 的 `handleTransformEnd(shape.id)` 還是照樣各自被呼叫、邏輯完全不用改。縮放控點固定都是四角（`CORNER_ANCHORS = ["top-left", "top-right", "bottom-left", "bottom-right"]`，不管選中的是 rect/image/文字還是多選，一律不給邊控點，不能只改寬或只改高）——靠選取變動的 `useEffect` 呼叫共用的 `applyTransformerTarget(transformer, nodes: Konva.Node[], singleSelectedLockAspectRatio: boolean)` 動態切換共用 `Transformer` 的 `keepRatio`。**`UNIFORM_SCALE_CLASS_NAMES` 目前只剩 `Text`/`Star`**（`Circle`/`RegularPolygon` 已經拿掉，見下方「鎖定高寬比」條目）：單一選取時，如果節點類型在這個 Set 裡（天生強制等比，跟使用者設定無關）**或**呼叫端傳入的 `singleSelectedLockAspectRatio` 為 `true`（該 shape 自己的持久化 `lockAspectRatio` 欄位），就 `keepRatio(true)`，其餘所有情況（0 個、1 個沒鎖定的 rect/image/circle/triangle/line、2 個以上不論類型）`keepRatio(false)`。`singleSelectedLockAspectRatio` 由 `useShapeSelection.ts` 內部算好（`selectedIds.length === 1` 時查對應 shape 的 `lockAspectRatio`，否則一律 `false`——**多選刻意不處理各自尊重自己 `lockAspectRatio` 的情境**，維持跟既有多選邏輯一致的簡化範圍），選取變動的 `useEffect` 直接依賴這個值傳入；`registerShapeRef` 的 callback（空依賴陣列、跨 render 身分穩定）讀不到最新值，額外用 `singleSelectedLockAspectRatioRef`（`useRef` + 對應 `useEffect` 同步）轉一手，跟 `selectedIdsRef` 同樣的「避免 stale closure」手法。`Star` 依然會被鎖死等比，是 Konva 本身的限制（`Star` 的 `width`/`height` getter/setter 都綁死同一顆 `outerRadius`，`scaleX !== scaleY` 時只會有其中一個維度真的生效，跟使用者拖曳的手感對不上），這次改動刻意不處理，見下方新形狀條目；`Circle`/`RegularPolygon`（三角形）已經改成可以自由（非等比）拉伸，不再屬於這個限制。`CORNER_ANCHORS` 原本是 rect/image 專用的 8 控點跟文字專用的 4 角控點兩個獨立常數，拿掉 rect/image 的 4 個邊控點後兩者變成同一組，合併成一個常數、`enabledAnchors` 不再需要依類型分流。圖片是非同步載入完成才會有 Konva node，`registerShapeRef` 的 callback 內有補一段「node 剛掛上時如果自己也在目前選取集合裡就重新收集所有選中節點、手動 attach 一次 Transformer」的邏輯（靠 `selectedIdsRef` 讀最新的選取陣列，避免 get-or-create 快取住的 callback 抓到 stale closure），不然「新增圖片後自動選取」會變成「選了但看不到控點」。**刪除 shape 時的 ref Map 清理是雙重機制**：① `registerShapeRef` 的 `node === null` 分支（Konva node unmount 時觸發）清掉 `shapeNodesRef`/`shapeRefCallbacksRef` 對應 entry；② 另外還有一個以 `shapes` 為依賴的保底 `useEffect`，每次 `shapes` 變動時比對「目前還存在的 id」清掉 Map 裡的殘留——這是因為 `URLImage` 在圖片非同步載入完成前 `return null`，如果圖片還沒載入完成就被刪除，它的 ref callback 從頭到尾不會被呼叫過，只靠 ①會漏掉這個情況、造成小型 memory leak。**這支 hook 雖然放在 `src/hooks/`，內部仍然直接依賴 Konva（`Konva.Node`/`Konva.Transformer`/`KonvaEventObject`），不是脫離 Konva 也能用的通用邏輯**——`hooks/` 目前的慣例是所有自訂 hook（不論是否依賴 Konva）都放這裡，`KonvaSkill/` 則專放 Konva 畫布元件本體。`handleTransformEnd` 縮放矩形時，如果新的 `width`/`height` 讓 `cornerRadius`（見下方 `ShapePropertiesForm` 條目）超過新尺寸允許的上限，會順手把 `cornerRadius` 一併夾回合法範圍再一起 `updateShape`——不這樣做的話，圓角會停留在跟新尺寸不成比例的舊值，畫面上圓角看起來會重疊/異常，且只有使用者之後自己打開屬性面板對 `cornerRadius` 欄位觸發一次 blur 才會被動修正。
- **`panelRight` 的 `SelectedShapePanel` 底下有一個可編輯的 `ShapePropertiesForm`（`src/components/canvas/SelectedShapePanel/ShapePropertiesForm.tsx`，欄位元件在 `src/components/ui/` 底下）**：選中物件後除了既有的 ID pill，還會依 `shape.type` 顯示並可編輯該物件的屬性，欄位標籤直接用 Konva 的屬性名稱（不翻譯成中文）——`x`/`y`/`rotation` 每種 type 都有，接著 Rect/Image/Circle/Triangle 這四種「有獨立 width/height 概念」的 shape 共用同一段 JSX：顯示 `width`/`height`（Circle/Triangle 已經從舊的單一 `size` 改成跟 Rect/Image 一樣的獨立兩個欄位，見上方新形狀條目）+ 一顆 `IconAspectRatio` 的鎖定高寬比切換鈕（`lockAspectRatio`，四種都有），Rect 額外多顯示 `fill`/`cornerRadius`，Image 沒有 `fill`/`cornerRadius`，Circle/Triangle 額外顯示 `fill`（沒有 `cornerRadius`）；Star 維持舊行為，顯示兩個背後讀寫同一個 `size` 的 `width`/`height` 欄位 + `fill`，沒有鎖定高寬比切換鈕（這次不開放拉伸，見上方新形狀條目）；Line 顯示 `stroke`/`strokeWidth`（沒有 `fill`、也沒有尺寸欄位、也沒有鎖定高寬比，縮放只能靠 Transformer 拖曳）；Text 顯示 `fontSize`/`fill`/`text`/`bold`/`underline`/`strikethrough`（後三個是 `IconButtonUI` 切換鈕排成一列，不是 `NumberField`/`ColorField`，也沒有鎖定高寬比——概念上靠單一 `fontSize` 縮放，沒有寬高比可言）。呼叫端（`SelectedShapePanel.tsx`，只在「有明確目標」的分支渲染——`selectedIds.length === 1`，或 `selectedIds.length >= 2` 但 `activeId` 剛好還在選取裡，見上方架構重點的 `activeId` 條目）用 `key={id}`（`id` 優先取 `activeId`，沒有才回退成 `selectedIds[0]`）掛這個表單，切換選取到不同物件（尤其換了 type，欄位種類整批不同）時所有欄位的本地 state 靠 remount 整批歸零，沿用 `ExportModal` 掛 `key={String(open)}` 的既有慣例。**`x`/`y`/`width`/`height` 全部強制只能整數**：面板的 `NumberField` 都傳 `round`，且 `useShapeSelection.ts` 的 `handleDragEnd`（拖曳）/`handleTransformEnd`（Transformer 縮放）也把讀出來的座標/尺寸 `Math.round()` 過再寫回 `updateShape`——三個寫入來源（面板打字、拖曳、縮放控點）都維持同一個「永遠是整數」的不變量，不是只有面板單獨擋。**核心設計問題是 `updateShape` 每次呼叫都會 `pushHistoryEntry()`**：如果輸入框用 `onChange` 逐字元直接 commit，打「120」會產生 3 筆 undo entry 把 undo 功能打爛、打字本身也會因為每個按鍵都觸發一次全畫布重繪而不流暢，所以 `ui/NumberField/NumberField.tsx`/`ui/TextField/TextField.tsx` 都透過共用的 `src/hooks/useFieldDraft.ts` 走「本地 draft state，`onBlur`/Enter 才真正 commit」——沒有 focus 時永遠顯示外部最新值（處理拖曳/Transformer 縮放在面板未 focus 時把 `x`/`y`/`width`/`height`/`fontSize` 改掉的情境，不能讓面板卡住舊 draft），focus 時顯示本地 draft，blur 時 parse 失敗（空字串、非數字）就放棄 commit（不會把 `NaN` 寫進 `updateShape`，欄位自動還原成目前值），值沒變也不 commit（不多推無意義的 undo）。`NumberField` 的 Enter 直接 `blur()` 委派給同一份 commit 邏輯；**`ui/TextField/TextField.tsx`（`text` 欄位）的 Enter 行為不同——`commit({ keepEditing: true })`，送出目前內容但不失焦**（多行文字通常打完一段還想繼續編輯），並 `preventDefault` 擋掉 Enter 在 textarea 裡插入換行的預設行為，改用 Shift+Enter 打換行（`text` 支援 `"\n"`）；`useFieldDraft` 的 `commit` 因此多一個 optional 的 `keepEditing` 參數，預設（`NumberField`/`ColorField`/`TextField` 的 `onBlur`）都是 `false`（跟原本行為一致，commit 後 `isEditing` 設回 `false`）。`text` 欄位仍然擋掉空字串（trim 後長度 0 視為無效輸入放棄 commit）——文字內容一旦變成空字串在 Konva 裡幾乎是零面積節點，一旦提交就很難再點擊選中。**`ui/ColorField/ColorField.tsx`（`fill` 欄位）是唯一不經過 `useFieldDraft` 的欄位**：用 `<input type="color">` 直接受控於 `shape.fill`，`onChange` 直接 commit——已知風險是主流瀏覽器在色盤上拖曳選色時原生 `input` 事件會連續觸發，理論上會連續推進多筆 undo entry，這次先接受這個風險，備案是改監聽原生 `change` 事件（只在放開滑鼠/確定選色時觸發一次）才 commit。**`bold`/`underline`/`strikethrough` 這三個布林切換鈕也不經過 `useFieldDraft`**，理由跟 `ColorField` 一樣——單次點擊、立即生效，不是連續輸入/拖曳，沒有中間態需要 draft 緩衝，`onClick` 直接 `commit({ bold: !shape.bold })` 這樣取反寫回；`IconButtonUI` 為此新增 optional 的 `active` prop（`true` 時套用實心填色的 `.active` 樣式 + `aria-pressed`，`Toolbar.tsx` 既有的 Reset/Undo/Redo/Lock 這類一次性動作按鈕不傳這個 prop，行為不變）。`KonvaBoard.tsx` 把這三個布林值組合成 Konva 認的 `fontStyle`（`shape.bold ? "bold" : "normal"`）跟 `textDecoration`（`[underline && "underline", strikethrough && "line-through"].filter(Boolean).join(" ")`——Konva 內部用 `.indexOf('underline')`/`.indexOf('line-through')` 判斷，空白分隔可以同時套用兩種，不是只能二選一）。`cornerRadius` 的上限是動態算 `Math.min(shape.width, shape.height) / 2`（不是固定常數，因為圓角超過短邊一半在視覺上就不合理），`NumberField` 內部「先 `Math.round` 再 clamp」，避免上限剛好是奇數/2 這種小數時被四捨五入拉超界。`MIN_SHAPE_SIZE`/`MIN_FONT_SIZE` 這兩個 clamp 下限收在 `src/constants/shapeConstraints.ts`（見下方目錄結構），`useShapeSelection.ts` 的 Transformer 縮放邏輯跟面板欄位共用同一份，避免兩處下限各自維護、將來不同步。
- **`URLImage`（`src/components/canvas/KonvaSkill/URLImage.tsx`）用 `forwardRef` 轉發 ref**：`react-konva` 的 `<Image>` 需要吃已載入好的 `HTMLImageElement`，不是 src 字串，所以包一層元件內部用 `useEffect` 載入（含 `cancelled` guard 防止 unmount 後或 src 變動時的 stale setState）。**這裡的 `forwardRef` 不能省**：`registerShapeRef` 要拿到的是內部真正的 Konva `<Image>` 節點，不是 `URLImage` 這個 React 元件本身（普通 function component 不會自動轉發 ref）；漏了這步圖片會完全選不到、拖不動、縮放無反應，而且不會報錯，很容易漏測。
- **`KonvaBoard.tsx` 依 `shape.type` 分支渲染**，不能再用 `{...shape}` 整包展開到單一 `<Rect>`（現在有七種 Konva 元件：`Rect`/`Text`/`URLImage`/`Circle`/`RegularPolygon`/`Star`/`Line`），每個分支共用同一組 `{ draggable, onClick, onDragStart, onDragMove, onDragEnd, onTransformEnd }`（`commonHandlers`），避免把 `type` 這種 app 專用欄位洩漏成 Konva 節點屬性；`rect` 沒有寫明確的 `shape.type === "rect"` 判斷式，是最後隱含的 else 分支，跟原本三種型別時期的寫法一致。
- **Shape 按鈕改成圖形選單**（`types/shape.ts` 的 `CircleShape`/`TriangleShape`/`StarShape`/`LineShape` + `CanvasContext.tsx` 的 `addCircle`/`addTriangle`/`addStar`/`addLine`/`isShapePickerOpen` + 新的 `ShapePicker.tsx`）：`TemplatePanel.tsx` 的 Shape 按鈕不再直接呼叫 `addSquare`，改成切換 `isShapePickerOpen`（`setIsShapePickerOpen(!isShapePickerOpen)`），`SelectedShapePanel.tsx` 在最前面判斷這個旗標，是 `true` 就整個 return `<ShapePicker />`（優先權比選取資訊高，不管目前有沒有選取東西），使用者選一個形狀後 `ShapePicker` 的 `handleSelect` 呼叫對應的 `addXxx`；六個選項裡正方形/圓形/三角形/星形/直線的圖示直接拿 `constants/shapeTypeIcons.ts` 的 `SHAPE_TYPE_ICONS`（跟 `LayersPanel` 共用同一份 type→icon 對應，不用各自 import 一次 `@tabler/icons-react`），只有虛線選項專用的 `IconLineDashed` 直接 import——`"line"` 這個 type 沒有分虛實線兩個 icon，不屬於 `SHAPE_TYPE_ICONS` 的職責範圍。`isShapePickerOpen` 放進 `CanvasContext` 是因為 `TemplatePanel`（panelLeft，觸發開關）跟 `SelectedShapePanel`（panelRight，讀取並渲染）是平行元件，跟 `containerRef` 一樣的理由不能直接互傳 state；純 UI 暫態，不算進 undo history，跟 `selectedIds` 同類。**選單的關閉邏輯集中收斂在 `setSelectedIds` 本身**：`CanvasContext.tsx` 內部把 `useState` 回傳的原始 setter 改名成 `setSelectedIdsRaw`，對外曝露的 `setSelectedIds`是一層 `useCallback` wrapper，呼叫時一律先 `setIsShapePickerOpen(false)` 再真正 `setSelectedIdsRaw(update)`（型別維持 `Dispatch<SetStateAction<string[]>>` 相容，內部既有的 functional updater 用法不用改）——這樣 `selectShape`/`selectShapeExact`（畫布/圖層清單點擊）、`useShapeSelection.ts` 的 `handleStageMouseDown`/`finishMarquee`（點空白處、框選）、所有 `addXxx`（新增後自動選取）、`deleteShapes`、`resetCanvas`、`applySnapshot`（undo/redo）全部會呼叫到 `setSelectedIds` 的地方，選單都會自動關閉，不用在每個呼叫端各自記得補一句（**這是修過的 bug**：原本選單只有「選了選項」或「再點一次 Shape 按鈕」才會關，點 Text/Image 按鈕、在畫布上點選其他物件、undo/redo 都不會關，導致 panelRight 卡在選單、跟畫布實際狀態不同步）；`lockShapes`/`unlockShapes` 不會動到 `selectedIds`，另外各自補一句 `setIsShapePickerOpen(false)`，讓「任何會改變畫布/選取狀態的 action」都關閉選單，語意一致。因為 `setSelectedIds` 現在是真正的 `useCallback`（不是 React 原生保證恆定 identity 的 `useState` setter），所有把它放進依賴陣列的內部 action 都要老實列出來，`react-hooks/exhaustive-deps` 才不會噴警告。**三個關鍵的資料模型決定，已對照 Konva/react-konva 原始碼驗證過**：
  1. **`StarShape` 只存一個 `size`（直徑），不存 `width`/`height`**——因為 `Konva.Star` 把 `width`/`height` 的 getter/setter 都綁死成同一顆底層 `outerRadius` 換算（都回傳 `outerRadius() * 2`），硬存兩個獨立欄位只會讓資料失真（兩個值視覺上永遠被逼成同一個），`KonvaBoard.tsx` 宣告式渲染因此直接傳 `innerRadius`/`outerRadius`（Konva 認的「真正」幾何參數），不透過 `width`/`height` 這層便利介面繞；`useShapeSelection.ts` 的 `applyTransformerTarget` 把 `Star`（連同 `Text`）收進 `UNIFORM_SCALE_CLASS_NAMES`，單一選取時強制 `keepRatio(true)`——不然 `scaleX !== scaleY` 時只有其中一個維度會真的生效，縮放結果會跟拖曳手感對不上。星形的 `numPoints`（固定 5）、內外半徑比例（固定 `innerRadius = size / 4`）目前不開放調整，寫死在 `KonvaBoard.tsx`。`ShapePropertiesForm.tsx` 對星形刻意顯示兩個獨立的 `width`/`height` 欄位（不是單一 `size` 欄位），跟 Rect/Image 的介面一致、方便直接輸入調整——但兩個欄位背後讀寫的都是同一個 `commit({ size: v })`，改其中一個之後另一個顯示值會自動同步（沒有 focus 的那個欄位靠 `useFieldDraft`「沒 focus 時永遠顯示外部最新值」的既有機制自然更新），等比縮放的事實不用另外加提示文字，使用者操作幾次就會自然發現。
  1a. **`CircleShape`/`TriangleShape`（`Konva.RegularPolygon`，`sides=3` 的正三角形）已經改成可以獨立拉伸成橢圓/不等邊三角形**（跟 `RectShape`/`ImageShape` 一樣存 `width: number` + `height: number`，不再是單一 `size`）——這是後來推翻先前「Konva 幾何 getter/setter 綁死同一顆半徑」限制的改動，做法不是換成 `Konva.Ellipse`，而是**讓 `radius`/`sides` 這些 Konva 認的幾何參數保持固定**（`KonvaBoard.tsx` 的 `SHAPE_BASE_RADIUS` 常數，值等於 `CanvasContext.tsx` 的 `SHAPE_DEFAULT_SIZE / 2`，新增時 `scaleX`/`scaleY` 剛好都是 1），**實際 `width`/`height` 全部靠 `scaleX = shape.width / (SHAPE_BASE_RADIUS * 2)`／`scaleY = shape.height / (SHAPE_BASE_RADIUS * 2)` 這層變形矩陣表現**——Konva 是先用固定半徑畫完 path，才套用 `scaleX`/`scaleY`，所以 `scaleX !== scaleY` 時圓形照樣會被拉伸成橢圓、正三角形照樣會被拉伸成不等邊三角形，不需要換元件。這個技巧的關鍵副作用是 `node.width()`/`node.height()`（Konva 內建 getter）從此永遠讀到同一個常數（`SHAPE_BASE_RADIUS * 2`），因此 `useShapeSelection.ts` 的 `handleTransformEnd` 可以直接把 `Circle`/`RegularPolygon` 併入 rect/image 共用的 `node.width() * scaleX` / `node.height() * scaleY` 公式，不需要再另外分支（見上方架構重點）。`CanvasContext.tsx` 的 `addCircle`/`addTriangle` 因此新增時給 `width: SHAPE_DEFAULT_SIZE, height: SHAPE_DEFAULT_SIZE`（取代原本的 `size: SHAPE_DEFAULT_SIZE`）。**新增的持久化欄位 `lockAspectRatio: boolean`**（預設 `false`，算進 undo history，不是暫態 UI 狀態）放在 `RectShape`/`ImageShape`/`CircleShape`/`TriangleShape` 這四種「有獨立 width/height 概念」的 shape 上（`TextShape` 靠 `fontSize`、`LineShape` 靠 Transformer 兩點控點、`StarShape` 這次不開放拉伸，都不需要）；`ShapePropertiesForm.tsx` 在這四種 type 的 width/height 欄位下面加一顆 `IconAspectRatio` 的 `IconButtonUI` 切換鈕，比照 `bold`/`underline`/`strikethrough` 的既有模式（不經過 `useFieldDraft`，`onClick` 直接 `commit({ lockAspectRatio: !shape.lockAspectRatio })`）。`useShapeSelection.ts` 的 `applyTransformerTarget` 因此不能再只吃 `Konva.Node[]`，額外多接一個 `singleSelectedLockAspectRatio: boolean` 參數，見上方架構重點段落的詳細說明。
  2. `CircleShape`/`TriangleShape`/`StarShape` 的 `x`/`y` 是**形狀中心點**（Konva 的 `_sceneFunc` 都以 `(0,0)` 為中心畫），跟 `RectShape`/`ImageShape`/`TextShape` 的 `x`/`y`＝**左上角**是不同的錨點語意——`CanvasContext.tsx` 的 `addCircle`/`addTriangle`/`addStar` 新增時置中畫布直接用 `x: canvasWidth / 2, y: canvasHeight / 2`，不是像 `addSquare` 那樣算 `(canvasWidth - size) / 2`。
  3. `LineShape` 存 `points: number[]`（相對 `(x, y)` 的本地座標，`Konva.Line` 慣例）+ `stroke`/`strokeWidth`/`dash?: number[]`（沒有 `fill`，線不是填色而是描邊；直線跟虛線是同一個型別，`dash` 沒值是實線，有值——例如 `CanvasContext.tsx` 的 `LINE_DASH_PATTERN`——是虛線），`(x, y)` 本身是**線段起點**，又是第三種錨點語意，不是左上角也不是中心點。`Konva.Line` 完全沒有實質的 `width`/`height`（`getWidth()`/`getHeight()` 是讀 `getSelfRect()` 算出來的唯讀衍生值，沒有對應的 `setWidth`/`setHeight`），所以 `handleTransformEnd` 對 `Line` 的分支不是改 `width`/`height`，而是把 `points` 陣列的每個座標依奇偶索引（x 分量／y 分量）分別乘上 `scaleX`/`scaleY`，算完會檢查新的兩端點距離，低於 `MIN_LINE_LENGTH`（直接沿用 `MIN_SHAPE_SIZE`）就把第二個端點沿原本方向拉回剛好等於下限的位置（兩端點剛好重合、方向不存在時退回預設水平線），維持跟其他形狀一致的「絕不縮到消失」下限 clamp。`ShapePropertiesForm.tsx` 也因此完全不給 `line` 顯示尺寸欄位（縮放只能靠 Transformer 拖曳），只給 `stroke`（`ColorField`）+ `strokeWidth`（`NumberField`）。**`Line` 的 Transformer 控點是 `LINE_ANCHORS = ["middle-left", "middle-right"]`，不是 `CORNER_ANCHORS`**（`useShapeSelection.ts` 的 `applyTransformerTarget` 依 `isSingleLine = nodes.length === 1 && nodes[0].getClassName() === "Line"` 判斷，多選——就算全部都是 line——維持四角，不特別處理）：Konva 只給這兩個控點時，拖曳只會改變 `scaleX`（沿節點本地 x 軸）、`scaleY` 維持 `1`，`LineShape` 預設插入是水平線（本地 x 軸方向），旋轉後這兩個控點會跟著節點一起轉，體感就是「拉線段自己的方向」而不會拉出形變，`rotateEnabled` 是獨立設定不受影響。`KonvaBoard.tsx` 的 `<Line>` 也加了 `strokeScaleEnabled={false}`——Konva 預設縮放時筆畫粗細會跟著 `scaleX`/`scaleY` 視覺拉伸，`onTransformEnd` 把 scale reset 回 1 卻沒同步調整 `strokeWidth`，不關掉的話放開控點那一刻線條粗細會「跳」一下。
- **KonvaBoard 的畫布本體背景是 `listening={false}`**：那塊白色 `Rect`（`canvasWidth`×`canvasHeight`）只是視覺背景，特地關掉事件監聽讓點擊穿透到 `Stage`，`useShapeSelection` 的 `handleStageMouseDown` 才能靠 `e.target === e.target.getStage()` 正確判斷「點到畫布空白處」；如果拿掉 `listening={false}`，點在畫布內、沒有物件的地方會被這塊背景攔截，變成「點不到空白處」。
- **多選：shift/ctrl+click 加選、框選（marquee）、圖層鎖定分組**（`useShapeSelection.ts` + `CanvasContext.tsx` + `Toolbar.tsx` + `LayersPanel.tsx`）：畫布上點 shape 一律經過 `handleSelect(id, e)` → `selectShape(id, isAdditiveClick(e.evt))`；`selectShape` 若發現點到的 shape 有 `groupId` 且該 `groupId` 在整個 `shapes` 裡實際命中 2 個以上成員，選取單位（`selectedIds`）會展開成整個分組（不是只選那一個 id），讓使用者在畫布上點分組裡任一成員都能看到/操作整組範圍——共用的 Transformer 因此會直接框住整組，不用額外改動。**`activeId` 是跟 `selectedIds` 分開的第二個選取狀態**：`selectShape` 算出展開/切換後的最終選取陣列（`nextIds`）後，**只有這次選取單位真的因為分組而展開**（`idsToToggle.length > 1`）**且**使用者實際點的那個 `id` 還在 `nextIds` 裡，才把 `activeId` 設成它，否則一律設成 `null`——`panelRight` 靠這個判斷「選到 2 個以上時該顯示整組批次摘要，還是使用者點的那一個 shape 的個別編輯表單」（見下方 `SelectedShapePanel` 獨立條目），解決「點分組成員後選取變成整組，導致沒辦法單獨編輯分組裡某一個成員」的問題。**這個判斷條件刻意排除一般多選**（shift/ctrl+click 疊加沒有分組的 shape，`idsToToggle` 永遠是長度 1 的 `[id]`）——這是修正過的 bug：一開始只看「id 還在不在 nextIds 裡」，導致一般 shift+click 累加出的多選也被誤判成「有明確目標」，讓「N shapes selected + 批次刪除」這個既有介面在最常見的多選流程下完全顯示不出來。`selectShapeExact`（見下方獨立條目）永遠不會展開分組，因此永遠把 `activeId` 設成 `null`。框選（`finishMarquee`）跟空白處點擊清空選取（`handleStageMouseDown`）也都沒有「使用者點了哪一個特定 shape」這種明確目標，一律把 `activeId` 設成 `null`，交給 `selectedIds.length` 決定面板要顯示什麼。**`LayersPanel` 的列點擊刻意不共用這支 `selectShape`，改呼叫語意類似但不展開成整組的 `selectShapeExact(id, additive)`**：圖層清單已經是「列出每個獨立 shape」的介面，點特定一列就是想針對那一個 id 操作，即使它屬於鎖定分組也要能單獨選到、單獨編輯它的 `x`/`y`/`fill`/`cornerRadius` 等屬性（`SelectedShapePanel` 只有 `selectedIds.length === 1` 才會顯示可編輯的 `ShapePropertiesForm`，如果清單點擊也展開成整組，使用者就再也無法透過點擊單獨選到分組裡的某一個成員來編輯）——兩支函式都是 `toggleSelection(prev, ids, additive)`（`src/utils/selection.ts` 的純函式，`additive` 為 false 直接取代選取，true 時整批加入/移出）的薄包裝，差別只在 `selectShape` 傳整組展開後的 `ids`、`selectShapeExact` 永遠傳 `[id]`。**空白處點擊（`handleStageMouseDown`）是已知的不對稱行為**：一律直接 `setSelectedIds([])` 清空，不管有沒有按 shift／ctrl，也不會跟框選做「加選」合併——這是這輪刻意簡化的範圍，之後如果要讓兩種入口的 additive 語意完全一致，要在這裡也讀 `isAdditiveClick` 並讓框選的收尾邏輯決定要不要跟原本的 `selectedIds` 合併。**框選（marquee）**：`handleStageMouseDown` 在點到空白處時記錄起點（`stage.getRelativePointerPosition()`，跟 shape 的 `x`/`y` 同一個座標系，不受 `Stage` 縮放/平移影響），`handleStageMouseMove` 持續更新框的範圍並畫出一個 `listening={false}` 的虛線 `Rect`（`KonvaBoard.tsx`），放開時（`handleStageMouseUp`）依框跟每個 shape 的 `node.getClientRect({ relativeTo: stage })` 是否相交（`src/utils/geometry.ts` 的 `rectsIntersect`）決定選取結果；拖曳距離小於 `MARQUEE_DRAG_THRESHOLD`（4px，畫布座標系）視為單純點擊，不觸發框選。**已知邊界情況（已修但值得注意）**：Konva 的 `mouseup` 只綁在 Stage 的 DOM container 上，如果框選拖曳途中把滑鼠移出容器才放開左鍵，`mouseup` 不會觸發——`handleStageMouseMove` 因此會額外檢查 `e.evt.buttons`（目前仍按著的按鍵），一偵測到左鍵已放開就直接呼叫跟 `handleStageMouseUp` 共用的 `finishMarquee` 收尾，避免框選矩形卡住跟著游標一直跑。**Transformer 多選**：選取變動的 `useEffect` 把 `selectedIds` 對應到的所有 Konva node 一次丟給 `transformer.nodes([...])`，Konva 原生支援對多個節點做群組縮放/旋轉，`handleTransformEnd` 完全不用改（見上方 `useShapeSelection` 條目）。**圖層鎖定分組**：`Toolbar.tsx` 在 Redo 旁邊只在 `selectedIds.length >= 2` 時顯示 `IconLock`/`IconLockOpen`，判斷邏輯是「目前選取是否剛好等於某個既有群組的全部成員」（都有同一個非空 `groupId`，且該 `groupId` 在整個 `shapes` 裡的成員數剛好等於 `selectedIds.length`，不多不少）——是的話顯示 `IconLockOpen`，點擊呼叫 `unlockShapes(selectedIds)` 只清 `groupId`；否則顯示 `IconLock`，點擊呼叫 `lockShapes(selectedIds)`：產生新 `groupId`（`nextId("group")`）蓋在選取的 shapes 上，並且**把它們搬到彼此相鄰的位置**（原本最小 index 那個點，保留彼此相對順序），因為這輪還沒有圖層清單的拖曳排序可以手動調整順序，鎖定當下不順手搬過去的話，`LayersPanel` 就沒辦法用一個框把不相鄰的圖層框在一起。`LayersPanel.tsx` 清單第一筆＝畫布最上層（蓋住其他物件），最後一筆＝畫布最下層（見下方「圖層清單拖曳排序」條目的顯示順序/資料順序說明），渲染每個 shape 的 icon（依 `type` 查共用的 `constants/shapeTypeIcons.ts` 的 `SHAPE_TYPE_ICONS: Record<CanvasShape["type"], TablerIconComponent>`——這份表也給 `ShapePicker.tsx` 共用，不用各自 import `@tabler/icons-react` 重複組同一份對應；不是走 `ui/icon-map`，那份是給資料驅動的 icon 名稱查表用，這裡的 icon 完全由 type union 決定）+ `id`，並把連續（陣列相鄰）且共用同一個非空 `groupId` 的區段（長度 ≥ 2）框成一個 bracket 群組（分組內部列表顯示順序也遞迴套用「第一筆＝最上層」規則）；`selectedIds` 命中的列用 `$color-button-outline-border` 高亮，跟 bracket 的 `$color-toolbar-border` 中性邊框刻意用不同顏色，避免「選取」跟「分組」兩種視覺意義混在一起。**這輪明確不做的事**：框選（marquee）展開成整組（`finishMarquee` 框到分組其中一個成員不會連帶選到其他成員，跟畫布點擊的展開行為不一致，是刻意先不處理的範圍）。拖曳排序圖層清單已經實作，見下一條。**已知限制**：`selectShapeExact` 讓使用者可以透過圖層清單把選取縮小成分組裡的單一成員（`selectedIds.length === 1`），這時 `Toolbar` 的 Lock/Unlock 按鈕會因為 gate 條件是 `selectedIds.length >= 2` 而完全不顯示——使用者明知道這個 shape 屬於鎖定分組，卻暫時看不到任何鎖定相關按鈕，這不算邏輯錯誤，但之後如果要處理，可以考慮讓 Lock 區塊在「剛好選到某個已分組成員」時也顯示（例如顯示 `IconLockOpen` 讓使用者能直接解除整組，即使目前只選了其中一個）。**鎖定分組後在畫布上拖曳其中一個成員，會連動其他成員一起移動**，見下一條。
- **圖層清單拖曳排序**（`LayersPanel.tsx` + `CanvasContext.tsx` 的 `reorderShapes`，用 `@dnd-kit/core`/`@dnd-kit/sortable`/`@dnd-kit/utilities` 實作）：把既有的 `buildRenderGroups(shapes)` 算出的每個「渲染單位」（`kind: "single"` 或 `kind: "group"`）當成 dnd-kit `useSortable` 管理的單一 sortable item——**不是**把每個 shape 各自變成 sortable item。這個做法完全繞開了先前研究過「dnd-kit 沒有內建多選整塊拖曳」的限制：鎖定分組的相鄰（contiguity）本來就已經是 `shapes` 陣列的結構性不變量（`lockShapes` 保證），不需要靠 dnd-kit 去理解「這幾個各自獨立選取的項目要黏在一起拖」，dnd-kit 從頭到尾只看到一份普通的扁平可排序清單。unit id 用 `shape.id`（single）或 `groupId`（group，來自 `nextId("group")`，跟 `shape-N`/`text-N`/`image-N` 前綴不同不會撞號）。**「資料順序」跟「顯示順序」是刻意分開的兩個陣列**：`ascendingGroups = buildRenderGroups(shapes)` 是資料順序（陣列原始順序，跟 `shapes` 本身的 z-order 語意一致），`displayGroups = [...ascendingGroups].reverse()` 是畫面由上到下實際渲染的順序（第一筆＝畫布最上層）；`unitOrder`（餵給 `SortableContext items`）跟實際渲染都用 `displayGroups`——dnd-kit 的排序動畫計算依賴 `items` 順序跟實際 DOM 順序一致，兩者必須同步。`handleDragEnd` 算完 `arrayMove` 後拿到的還是顯示順序，要再 `.reverse()` 一次換算回資料順序，才能用 `ascendingGroups` 建的 `unitMap` 展開回 shape id 陣列餵給 `reorderShapes`；分組內部（`group.shapes`）維持 `lockShapes` 建立的原本相對順序，不對它再做一次 reverse——分組內部的資料相對順序，跟 `SortableBracket` 為了畫面顯示另外做的 `[...shapes].reverse()`，是完全獨立的兩件事。`useSortable` 的 `attributes`/`listeners`/`setNodeRef`/`transform`/`transition` 掛在該 unit 的**外層元素**上——single 掛在 `LayerRow` 自己的 `.row` div（`LayerRow` 為此多了幾個 optional 的傳遞用 props：`rootRef`/`rootStyle`/`dragAttributes`/`dragListeners`/`isDragging`，分組裡的一般成員列完全不傳這些，行為不變），group 掛在新的 `SortableBracket` 元件擁有的 `.bracket` 包裝 div；group 內每個成員的 `LayerRow` 完全不知道自己在被拖曳，pointer 事件從內層 `LayerRow` bubble 到外層才觸發拖曳，這樣「按住分組裡任一列拖曳，整組一起移動」不需要額外程式碼。`PointerSensor` 用 `activationConstraint: { distance: ROW_DRAG_ACTIVATION_DISTANCE }`（4px，螢幕座標系，跟 `useShapeSelection.ts` 的 `MARQUEE_DRAG_THRESHOLD` 同樣哲學但量綱不同——一個是螢幕實際移動 px，一個是畫布座標系 px）分辨「單純點擊」跟「拖曳」，確保 `LayerRow` 既有的 `onClick`/`selectShapeExact` 不受影響。`handleDragEnd` 只算出 unit 順序的 `arrayMove` 結果，再把每個 unit 展開回底下的 shape id 列表（single 貢獻自己、group 貢獻所有成員、維持原相對順序），一次呼叫 `reorderShapes(expandedIds)`——drop target 永遠是整個 unit，不可能是「分組裡的某個位置」，所以 `groupId` 的相鄰不變量不需要額外驗證就自動維持。沒有用 `DragOverlay`（面板內小型單一容器清單，預設「項目自己用 transform 跟著移動」已經足夠，分組的情況如果要用 `DragOverlay` 還得把一整組列複製一份塞進疊層，划不來）。`reorderShapes(orderedIds)`（`CanvasContext.tsx`）要求 `orderedIds` 是目前 `shapes` 的完整排列（數量相同、id 集合相同），驗證失敗或排列跟目前完全相同都靜默不做事（不推 history）——只由內部程式碼呼叫，不是使用者輸入，沿用 `lockShapes`/`unlockShapes`/`deleteShapes` 靜默 no-op 的慣例。`reorderShapes` 不動 `selectedIds`/`activeId`/任何 `groupId`，純粹是 `shapes` 陣列元素順序的重排。`.row`/`.bracket` 因為現在也是 `useSortable` 的 ref 目標，加了 `touch-action: none`（dnd-kit 文件建議，避免觸控裝置原生捲動跟拖曳手勢搶事件；已知取捨是觸控使用者在列上按住就無法捲動清單，這個 app 目前是滑鼠優先，先接受）。**已知限制（刻意選擇，不是遺漏）：只支援滑鼠/觸控拖曳，不支援鍵盤排序**——`sensors` 只註冊了 `PointerSensor`，沒有加 `KeyboardSensor`；但 `useSortable` 的 `attributes` 仍然會固定帶上 `tabIndex`/`role`/`aria-roledescription="draggable"`（dnd-kit core 內部邏輯，跟有沒有註冊哪個 sensor 無關），代表每一列在畫面上看起來「可以被 tab 到、標示成可拖曳元件」，但按方向鍵實際上完全沒反應——這跟專案目前整體偏低的 a11y 水準一致（`Modal` 也只做基本 a11y），先接受，之後如果要補鍵盤排序要加 `KeyboardSensor` + `sortableKeyboardCoordinates`。
- **鎖定分組的畫布拖曳連動**（`useShapeSelection.ts` 的 `handleDragStart`/`handleDragMove`/`handleDragEnd` + `CanvasContext.tsx` 的 `updateShapes`）：拖曳分組裡任一個成員（不需要事先多選，純粹看 `shape.groupId`），其他成員要跟著一起位移。**沒有把同 `groupId` 的 shapes 包進 `Konva.Group`**（那樣要把 `shapes` 陣列的絕對座標改成相對群組的座標，牽動的既有假設太多）——改用「記錄拖曳開始當下每個成員的起始座標，位移量套用到所有成員」的做法：`handleDragStart(id)` 找出 `shapes` 裡 `groupId` 相同（排除自己）的其他成員，把它們目前的 Konva node 座標記進 `groupDragRef`；`handleDragMove(id)` 只在目前正在拖曳的就是這個 ref 記錄的 lead 時才動作，算出 `e.target` 相對起點的位移量，直接呼叫 `node.position()` 搬動其他成員的 Konva node（純 imperative，不透過 `setShapes`，避免拖曳中途觸發 re-render），並手動呼叫 `transformerRef.current?.forceUpdate()`（因為 `node.position()` 不是 Konva 原生拖曳，不會自動觸發 Transformer 監聽的 `dragmove` 事件）；`handleDragEnd(id)` 才真正把 lead + 所有成員的最終座標一次透過新的 `updateShapes(patches)`（`CanvasContext.tsx`，`updateShape` 的批次版本）寫回，一次拖曳操作只推一筆 undo entry。`memberShapes.length === 0`（沒有 `groupId`，或 `groupId` 只剩自己一個成員）時完全走原本的單一物件 `updateShape` 路徑，行為跟沒有分組功能之前一致。`handleDragStart` 記錄成員起始座標時優先讀 Konva node（跟畫面實際位置一致），圖片非同步載入完成前沒有 node 就退回讀 `shape.x/y`——這個成員還沒有 node 可以在拖曳過程中即時跟著移動，但仍然會留在 `members` 名單裡讓 `handleDragEnd` 把算出的最終座標一起寫回，不然圖片載入完成後會停在群組拖曳前的舊位置。**已知限制**：這次只有拖曳（`handleDragStart`/`handleDragMove`/`handleDragEnd`）做了群組連動，`handleTransformEnd`（縮放/旋轉）完全沒有比照處理——縮放或旋轉鎖定分組裡的某個成員，只有它自己會變形，其他成員留在原地不動。
- **對齊功能**（`Toolbar.tsx` + `CanvasContext.tsx` 的 `alignShapes(ids, mode)`）：`Toolbar` 在 Lock/Unlock 按鈕右邊、`selectedIds.length >= 1` 時顯示 6 顆對齊按鈕（`ALIGN_BUTTONS` 常數，`IconLayoutAlignLeft`/`Center`/`Right`/`Top`/`Middle`/`Bottom`，`IconButtonUI` 呈現，比照 Undo/Redo/Lock 的擺放風格，中間補一條 `.divider`），每顆按鈕呼叫 `alignShapes(selectedIds, mode)`，`AlignMode` 是 `CanvasContext.tsx` 匯出的 `"left" | "center-h" | "right" | "top" | "center-v" | "bottom"` 型別。**每個被選取的 shape 各自獨立對齊到畫布邊界/中心，不是選取物件彼此對齊**——`alignShapes` 對每個 id 各自算位移量：靠 `stageRef.current.findOne('#' + id)` 找到對應的 Konva node（`KonvaBoard.tsx` 因此在每個渲染的 Konva 元素上都加了 `id={shape.id}`，之前只有 `key` 沒有 `id`，純粹是給 `alignShapes` 這個用 CSS 選擇器查找節點的新需求補上的），對 `node.getClientRect({ relativeTo: stage })` 拿到畫布座標系包圍盒，跟 `canvasWidth`/`canvasHeight` 比較算出 `deltaX`/`deltaY`（例如 `left` 是 `-rect.x`、`center-h` 是 `(canvasWidth - rect.width) / 2 - rect.x`），直接加到該 shape 目前的 `x`/`y`——**位移量對任何 shape 的 x/y 錨點語意都通用**（不用理會 rect/image 是左上角、circle/triangle 是中心點、line 是線段起點這幾種不同語意，見上方新形狀條目），最後把所有選取 shape 的新 `x`/`y` 一次透過 `updateShapes` 寫入，一次操作只推一筆 undo entry。之所以不透過 `useShapeSelection.ts` 內部的 `shapeNodesRef`（理論上查找 O(1) 比 `findOne` 快），是因為那個 Map 是該 hook 的私有 state，只有 `KonvaBoard.tsx` 呼叫過一次 `useShapeSelection()`；`Toolbar` 是平行元件，若在這裡也呼叫一次 `useShapeSelection()` 只會拿到另一份空的、從未被 `registerShapeRef` 填過的 Map，不能真的共享節點——`stageRef` 本來就已經在 `CanvasContext` 裡跨元件共享（給匯出功能用），透過它 `findOne` 是不需要額外狀態就能讓 `Toolbar` 拿到 Konva node 的最小改動。找不到 shape 或找不到對應 node（極少數情況，例如圖片還沒載入完成沒有 node）就跳過那個 id，不會讓整批操作因為單一 shape 失敗而整個中止（**已知限制**：不像 `handleDragStart` 那樣 fallback 讀 `shape.x/y`，這種情況下該 shape 這次對齊完全不生效；發生機率低，先接受）。**`alignShapes` 也會呼叫 `setIsShapePickerOpen(false)`**（只在至少有一筆有效 patch 時，比照 `lockShapes`/`unlockShapes` 手動補一句，不經過 `setSelectedIds`）——這是修過的 bug：`alignShapes` 不動 `selectedIds`，一開始沒有補這句，導致「先選取物件 → 點 Shape 按鈕打開圖形選單 → 點 Toolbar 對齊按鈕」這個路徑下，物件已經被對齊挪動，但 `panelRight` 還卡在 `ShapePicker`，跟畫布實際狀態不同步，是跟「Shape 按鈕改成圖形選單」條目描述的同一類問題。**已知限制（刻意不修）**：鎖定分組的成員如果被一起選取後執行對齊，每個成員會各自獨立對齊到畫布邊界/中心，不會維持彼此的相對排列（跟拖曳分組時「整組一起位移」的連動行為觀感不一致）——這次對齊功能只處理「每個 shape 各自對齊」，沒有比照鎖定分組拖曳連動另外處理群組整體對齊。
- **元件組裝順序**：`CanvasWorkspace`（`src/components/canvas/CanvasWorkspace/CanvasWorkspace.tsx`）是唯一的畫布頁面組裝點，把 `Toolbar` + `panelLeft`（`LayersPanel` + `TemplatePanel`）+ `KonvaBoard` + `panelRight`（`SelectedShapePanel`）包在 `CanvasProvider` 裡。`src/app/canva/page.tsx` render `CanvasWorkspace`；`src/app/page.tsx`（首頁）改成直接 render `CanvaPage`（`canva/page.tsx` 的 default export），不是另外重複 import `CanvasWorkspace`——兩個路由現在共用同一份組裝邏輯。**`panelLeft` 內部是「上方可捲動 + 下方固定」版面**：`LayersPanel` 是 `flex: 1; min-height: 0; overflow-y: auto`（`min-height: 0` 是必要的，flex item 預設 `min-height: auto` 不會縮小，圖層一多會撐爆父層把下面的按鈕擠出畫面，而不是自己內部捲動），中間一條 `panelDivider`，下面固定放 `TemplatePanel`（Image/Shape/Text 按鈕，內容不變）；`panelLeft` 自己蓋掉繼承自 `.panel` 的 `overflow-y: auto`（改成 `overflow: hidden`），改由 `LayersPanel` 自己捲動。
- **KonvaBoard 的縮放/平移**：`Stage` 的 `scale`/`stagePos` 只有一個來源——`ResizeObserver` 每次量到容器尺寸時（含 mount），都會重新算一個能讓畫布（`canvasWidth`×`canvasHeight`）完整放進容器並置中的 fit scale（見 `FIT_PADDING_RATIO`），依賴陣列包含 `canvasWidth`/`canvasHeight`，尺寸本身變動也會重新 fit。**滑鼠滾輪縮放已經移除**（`Stage` 沒有掛 `onWheel`）——之前有一版靠滾輪事件依游標位置縮放，之後會另外設計「調整畫布大小」的功能，這次先單純拿掉滾輪這個互動，`MIN_SCALE`/`MAX_SCALE` 兩個常數目前只給 fit-scale 邏輯當上下限用。
- **Icon 型別**：`@tabler/icons-react` 沒有匯出共用的 icon 元件型別，`src/components/ui/icon-map/index.ts` 用 `typeof IconHome` 反推出 `TablerIconComponent`，並以 `iconMap` 物件當 source of truth 反推 `IconName`。之後要用固定 icon 名稱查表時走這個 `iconMap`，目前 `templateList.ts` 是暫時直接 import icon 元件（都先用 `IconFileExport` 佔位）。
- **UI 元件的 variant 寫法**：`ButtonUI` 用 `selectedStyle`（`"origin" | "template"`）對應到不同 class，每個 variant 是完整獨立的樣式，不共用基底 class（見 `ButtonUI.module.scss`）——新增 variant 時比照這個結構加。
- **純 icon 按鈕用 `IconButtonUI`，不是 `ButtonUI` 加 boolean flag**：`ButtonUI` 的兩個 variant 都是「圖示+文字」設計；工具列常駐的純 icon 按鈕（Toolbar 的 Reset/Undo/Redo）視覺定位不同（ghost 風格、無邊框），硬把 `ButtonUI` 的 `name` 改成「有時顯示、有時只當 aria-label」會讓介面語意不直覺，所以另開 `src/components/ui/IconButtonUI/`，`label: string` prop 明確只映射 `aria-label`/`title`，不顯示文字。
- **`Modal`（`src/components/ui/Modal/`）內建「置中標題 + 右上角 X」的 header 版面**，不是讓呼叫端自己組 header/main 兩個 slot——`title`/`onClose` 兩個 prop 就能拼出這個幾乎所有 modal 都會用到的版面，之後加新 modal（例如確認刪除、畫布尺寸設定）直接傳 `title` 就好。用 `createPortal` 渲染到 `document.body`（不是原地渲染在呼叫端的 DOM 位置），避免任何未來祖先元素加 `transform`/`filter` 意外影響 `position: fixed` 的定位基準。`open === false` 時整個 `return null`（不是 CSS 隱藏）。**兩個容易踩到的坑，已經在程式碼裡處理掉**：① header 只有兩個子元素（標題、關閉鈕）時 `justify-content: space-between` **不會**讓標題視覺置中（標題會被推到最左邊），要用三欄 `grid`（`32px 1fr 32px`，左邊留一個跟關閉鈕等寬的空白佔位欄）才能讓標題對齊 header 整體寬度的幾何中心；② **`Modal` 卸載不等於呼叫端的 state 自動重置**——如果呼叫端（例如 `ExportModal`）在父層是「永遠掛著、用 `open` prop 控制」而不是條件渲染整個元件，`Modal` 內部 `return null` 只會卸載它 portal 出去的子樹，呼叫端自己的 `useState` 完全不受影響、不會重置。要做到「每次重新打開都重置成初始值」，不要在 `useEffect` 裡呼叫 `setState`（ESLint 的 `react-hooks/set-state-in-effect` 會擋，也會多一次 cascading render）——改成讓呼叫端在 `open` 狀態切換時傳不同的 `key` 掛這個元件，key 變了 React 就會整個卸載重掛，state 自然拿到新的初始值（見 `Toolbar.tsx` 掛 `ExportModal` 的寫法）。不做 focus trap，只做基本 a11y（`role="dialog"`、`aria-modal`、`aria-labelledby` 用 `useId()` 產生、Esc 關閉、關閉鈕 `aria-label`），跟目前專案的 a11y 水準一致。
- **`ExportModal`（`src/components/canvas/ExportModal/`）的「Export Image」已經接上真正的匯出/下載，並且打開 Modal 當下就會先顯示匯出預覽**：匯出邏輯是對 `stageRef`（`useCanvas()` 給的 `Konva.Stage` 實例）呼叫 `stage.toDataURL({ mimeType: "image/png" })`——KonvaBoard 現在依 `shapes[]` 動態切出多個 Konva Layer（見下方畫筆交錯排序條目），`toDataURL()` 會自動合成底下所有 Layer，不用逐一手動處理。**這個 dataURL 只在 Modal 打開的當下（元件掛載時，靠 `key={String(isExportModalOpen)}` remount）算一次，存進 `previewDataUrl` state 顯示成 `<img>` 預覽**，按「Export Image」直接複用同一份資料觸發下載，不會重新計算——保證預覽跟實際下載的檔案永遠一致。因為讀 `stageRef.current`/`overlayLayerRef.current` 這兩個 ref 不能在 render 當下做（`react-hooks/refs` 規則），這段邏輯放在 `useLayoutEffect`（不是 `useEffect`）裡，讓計算在瀏覽器畫出下一幀之前就完成，不會有「無法產生預覽」的文字先閃一下。**匯出/預覽已經不含 Transformer 選取框**：`useLayoutEffect` 呼叫 `toDataURL()` 前後會用 `overlayLayerRef`（`useCanvas()` 給的、指向 KonvaBoard 那個永遠最上層的 UI 覆蓋層 `Layer` 的 ref）先 `.hide()` 再 `.show()`（`try/finally` 包住，即使 `toDataURL()` 丟例外也保證 Modal 關閉後選取框會恢復可見）——`Konva.Stage._toKonvaCanvas()` 對每個 child layer 會先檢查 `isVisible()`，不可見就直接跳過，這是 Konva 官方認可的「匯出時排除某個 Layer」標準做法；整段 hide → toDataURL → show 同步執行，畫面上不會閃爍。**已知、仍未處理的限制**：匯出的仍然是「目前畫面看到的樣子」，包含當下的縮放/平移留白（`Stage` 的 `width`/`height` 是整個容器尺寸，不是單純 `canvasWidth`×`canvasHeight`），要做到「穩定 1:1、只匯出畫布內容」需要改成只對特定區域呼叫 `toDataURL()`，是另一個獨立的改動。`getSnapshot()`（JSON 字串）是形狀資料本身，不是點陣圖，跟圖片匯出是獨立機制。

## 目錄結構

```
src/
  app/
    page.tsx            # 首頁，render CanvaPage（canva/page.tsx 的 default export）
    canva/page.tsx       # /canva 路由，render CanvasWorkspace
    layout.tsx / globals.css
  components/
    canvas/
      CanvasWorkspace/   # CanvasWorkspace.tsx + CanvasWorkspace.module.scss
      Toolbar/           # Toolbar.tsx + Toolbar.module.scss（Export + Reset/Undo/Redo +
                          # Lock/Unlock 圖層群組 + 6 顆對齊按鈕，見上方「對齊功能」條目）
      TemplatePanel/     # TemplatePanel.tsx + 它專用的資料來源 templateList.ts
      SelectedShapePanel/ # 選取物件後顯示在 panelRight 的 pill：剛好選 1 個時是既有的
                          # ID + 刪除鈕 + ShapePropertiesForm（依 type 顯示可編輯屬性，
                          # 見上方架構重點；用到的欄位元件在 ui/ 底下）；選 2 個以上時
                          # 改顯示「N shapes selected」+ 批次刪除按鈕，不渲染表單
      LayersPanel/        # LayersPanel.tsx + .module.scss，panelLeft 上半部的圖層清單，
                          # 清單第一筆＝畫布最上層（顯示順序跟 shapes 陣列的資料順序
                          # 刻意相反），拖曳排序用 @dnd-kit/core + @dnd-kit/sortable，
                          # 見下方架構重點
      ShapePicker/        # ShapePicker.tsx + .module.scss，Shape 按鈕點下去時
                          # panelRight 顯示的圖形選單（正方形/圓形/三角形/星形/直線/
                          # 虛線），見上方架構重點的「Shape 按鈕改成圖形選單」條目
      ExportModal/        # 匯出用的 modal 內容（檔名輸入框 + Export Image 按鈕），
                          # 目前只是 UI 殼，還沒接真正的圖片匯出邏輯
      KonvaSkill/         # Konva 畫布「元件」本體：KonvaBoard.tsx、URLImage.tsx，
                          # 之後畫筆/橡皮擦等新畫布元件放這裡；
                          # 選取/縮放這類邏輯（含 Transformer 的操作邏輯）放 hooks/，
                          # 這裡只放 <Transformer /> 這個 JSX 元素本身
    ui/
      ButtonUI/          # ButtonUI.tsx + ButtonUI.module.scss（圖示+文字按鈕）
      IconButtonUI/      # IconButtonUI.tsx + .module.scss（純 icon 按鈕）
      InputUI/           # InputUI.tsx + InputUI.module.scss（綠框白底，ExportModal 第一個用它）
      Modal/             # Modal.tsx + .module.scss（通用 modal 殼，見上方架構重點）
      NumberField/       # NumberField.tsx + .module.scss（x/y/width/height/fontSize/
                          # cornerRadius 這類數字屬性欄位，見上方架構重點）
      ColorField/        # ColorField.tsx + .module.scss（fill 顏色欄位）
      TextField/         # TextField.tsx + .module.scss（text 內容欄位，debounce 同步）
      icon-map/index.ts
  context/
    CanvasContext.tsx    # 畫布狀態：shapes（CanvasShape union）、canvasWidth/canvasHeight、
                          # containerRef、addSquare/addText/addImage/addCircle/addTriangle/
                          # addStar/addLine、isShapePickerOpen（Shape 按鈕圖形選單開關，
                          # 見上方架構重點）、updateShape/updateShapes/deleteShape/deleteShapes/
                          # resetCanvas/reorderShapes（圖層清單拖曳排序，見上方架構重點）、
                          # alignShapes（對齊選取物件到畫布邊界/中心，見上方獨立條目）、
                          # selectedIds（多選，selectedId 是只有一個元素時的衍生值）、
                          # selectShape（畫布點擊用，點到鎖定分組成員會展開成整組）、
                          # selectShapeExact（圖層清單列點擊用，永遠只選那一個 id、不展開）、
                          # lockShapes/unlockShapes（圖層鎖定分組，見下方架構重點）、
                          # nextId（shape-1/text-1/image-1/group-1 計數器）、
                          # undo/redo/canUndo/canRedo（past/future history，見上方架構重點）、
                          # getSnapshot（給未來 Export 用，跟 undo/redo 是兩套獨立機制）
  types/
    shape.ts             # 畫布物件型別：RectShape（含 cornerRadius）、TextShape、ImageShape、
                          # CircleShape/TriangleShape（存 width/height，可獨立拉伸成橢圓/不等邊，
                          # x/y 是中心點，見上方新形狀條目）、StarShape（只存 size，沒有 width/height，
                          # x/y 是中心點，這次不動）、LineShape（points/stroke/strokeWidth/dash，
                          # x/y 是線段起點，見上方架構重點的「Shape 按鈕改成圖形選單」條目）、
                          # CanvasShape（union）、ShapePatch、CanvasSnapshot。RectShape/ImageShape/
                          # CircleShape/TriangleShape 都有 lockAspectRatio: boolean（持久化欄位，
                          # 算進 undo history，見上方新形狀條目）。全部 shape 都有
                          # optional 的 groupId（圖層鎖定分組用，見下方架構重點）
  styles/
    global.scss          # SCSS 共用變數（顏色、尺寸），給各 *.module.scss 用
  hooks/
    useShapeSelection.ts # 選取（含多選、框選 marquee）/拖曳/縮放（見上方架構重點），
                          # 依賴 Konva 但仍放這裡
    useFieldDraft.ts     # NumberField/ColorField/TextField 共用的 draft/commit 邏輯
                          # （見上方架構重點），不依賴 Konva
  constants/
    shapeConstraints.ts  # MIN_SHAPE_SIZE、MIN_FONT_SIZE——useShapeSelection.ts 的
                          # Transformer 縮放下限跟 SelectedShapePanel 的欄位輸入框共用
                          # 同一份，不屬於任一方專用，所以獨立成頂層目錄
    shapeTypeIcons.ts    # SHAPE_TYPE_ICONS（Record<CanvasShape["type"], TablerIconComponent>），
                          # 給 LayersPanel/ShapePicker 共用，同樣不屬於任一方專用
  utils/
    selection.ts         # toggleSelection/isAdditiveClick：純函式、不依賴 Konva 或 React，
                          # 畫布上的 shape 點擊（Konva event）跟圖層清單的 row 點擊（DOM
                          # MouseEvent）共用同一套「shift/ctrl 是否為加選」判斷邏輯
    geometry.ts           # Rect 型別 + rectsIntersect：框選（marquee）用來判斷選取框跟
                          # 每個 shape 的 client rect 是否相交，一樣是不依賴 Konva/React 的純函式
```

> 分資料夾的原則：① 元件與同名 `*.module.scss` 放進同名資料夾（例如 `Toolbar/Toolbar.tsx` + `Toolbar/Toolbar.module.scss`）；② 元件與它專用、只有自己會用的資料/邏輯檔放在一起（例如 `TemplatePanel/templateList.ts`）；③ `KonvaSkill/` 專放 Konva 畫布元件本體（`KonvaBoard.tsx`、`URLImage.tsx`），`hooks/` 放所有自訂 hook——不分是否依賴 Konva，`useShapeSelection.ts` 雖然直接用到 `Konva.Node`/`Transformer` 也放這裡，不因為依賴 Konva 就搬進 `KonvaSkill/`；④ `utils/` 放純函式（不依賴 Konva 或 React），給 context/hook/元件共用的邏輯——跟 `constants/` 一樣是「不屬於任一方專用」才獨立成頂層目錄，差別是 `constants/` 放常數值，`utils/` 放函式。單純沒有拆分需求的元件不用特地包資料夾。

## Konva / react-konva 慣例

- 任何引用 `react-konva` 的檔案，開頭必須加 `"use client"`。
- `Stage` 大小需響應容器尺寸變化（監聽 `resize` 或用 `ResizeObserver`），不要寫死 px。
- 縮放圖形時，`onTransformEnd` 一律要把 `scaleX/scaleY` 讀出來後重置回 1，並改寫 `width/height`（或 `fontSize`），避免下次拖曳時尺寸疊加跑掉。
- 橡皮擦透過 `globalCompositeOperation: "destination-out"` 實作，不要用「畫白色線」模擬（白色線在深色背景或匯出 PNG 時會出錯）。
- 可選取的物件（圖片、文字）用單一 `selectedId` 狀態管理，搭配一個共用的 `Transformer`，不要每個物件各自掛一個 Transformer 實例。
- 畫筆/橡皮擦產生的 `Line` 預設不可選取、不可拖曳，避免跟畫圖手勢衝突。

> 圖片（`URLImage`）、文字選取縮放、共用 `Transformer` 已經實作（見上方「架構重點」）。橡皮擦、`EditableText`（雙擊進入編輯模式）這兩條對應的功能還沒做，是後續開發時要遵守的慣例，不代表目前程式碼已有。

## 程式碼風格

- Function component + hooks，不使用 class component。
- 畫布物件狀態用 `CanvasShape`（`RectShape | ImageShape | TextShape`）union + `type` 判別欄位放單一扁平陣列，不要塞進同一個巢狀結構（例如 group-of-groups）；也不要拆成 `shapes`/`texts`/`images` 三個平行陣列——渲染迴圈、選取邏輯都要遍歷同一份資料，拆開只會讓查找/同步邏輯變複雜。
- 型別定義集中放在 `src/types/`，畫布物件（`RectShape`、`ImageShape`、`TextShape`，之後畫筆功能的 `Line`）都要有明確 interface。
- commit message 用 Conventional Commits（`feat:`, `fix:`, `chore:`, `refactor:`...）。

## 目前開發階段

- [x] Step 1：Next.js 專案初始化 + 依賴安裝
- [ ] Step 2：Web UI 設計（Toolbar、版面配置、色彩系統）
- [ ] Step 3：Konva 核心功能（畫筆、橡皮擦、上傳圖片、文字、Transformer）——上傳圖片/文字/Transformer 選取縮放/旋轉已完成，畫筆、橡皮擦尚未開始

> 每完成一個階段請更新這份清單，方便下次接續開發時快速掌握進度。

## 工作流程（必須遵守）

每次完成一項程式碼修改或新功能實作後，在回報「完成」給使用者之前，
**必須主動呼叫 `code-reviewer` subagent** 審查這次的改動，不需要使用者額外提醒。

流程固定為：

1. 完成實作
2. 自動呼叫 code-reviewer subagent 審查
3. 把審查結果（嚴重/建議/可忽略）整理後一併回報給使用者
4. 若 code-reviewer 標記「嚴重」等級問題，先修正後才算完成，不要略過直接回報完成

只有使用者明確說「跳過審查」或「先不用 review」時才可以省略這個步驟。

<!-- BEGIN:nextjs-agent-rules -->

# This is NOT the Next.js you know

This version has breaking changes — APIs, conventions, and file structure may all differ from your training data. Read the relevant guide in `node_modules/next/dist/docs/` (resolved from this file's directory; in monorepos the `next` package may not be visible from the repo root) before writing any code. Heed deprecation notices.

This block is written and re-added by `next dev` — verify at `node_modules/next/dist/server/lib/generate-agent-files.js`. Removing it from a diff only re-creates the uncommitted change; committing it with your work keeps the tree clean.

<!-- END:nextjs-agent-rules -->
